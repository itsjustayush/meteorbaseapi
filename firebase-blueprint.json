{
  "entities": {
    "ApiKey": {
      "title": "ApiKey",
      "description": "Secure API keys generated by users with rate limits and hash verification",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique key identifier" },
        "user_id": { "type": "string", "description": "Owner user UID from Firebase Auth" },
        "name": { "type": "string", "description": "Human readable label for the key" },
        "key_hash": { "type": "string", "description": "SHA-256 hash of the secret key token" },
        "key_preview": { "type": "string", "description": "Masked preview of the key e.g. meteor_live_...3f8a" },
        "created_at": { "type": "string", "format": "date-time", "description": "Creation timestamp" },
        "status": { "type": "string", "enum": ["active", "revoked"], "description": "Key status" },
        "rate_limit_per_day": { "type": "number", "description": "Daily request limit" },
        "rate_limit_per_min": { "type": "number", "description": "Per minute burst limit" },
        "usage_count": { "type": "number", "description": "Total lifetime requests made with this key" },
        "last_used_at": { "type": "string", "description": "Last time key was used to authenticate" }
      },
      "required": ["id", "user_id", "key_hash", "key_preview", "created_at", "status"]
    },
    "DynamicRecord": {
      "title": "DynamicRecord",
      "description": "Flexible schema supporting dynamic payloads, summaries, tasks, and future custom entities with automated 90-day retention",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique record identifier" },
        "created_at": { "type": "string", "format": "date-time", "description": "Creation timestamp" },
        "expires_at": { "type": "string", "format": "date-time", "description": "Expiration timestamp (90 days retention)" },
        "created_at_epoch": { "type": "number", "description": "Creation unix epoch timestamp in seconds" },
        "expires_at_epoch": { "type": "number", "description": "Expiration unix epoch timestamp in seconds" }
      },
      "required": ["id", "created_at"]
    }
  },
  "firestore": {
    "/api_keys/{keyId}": {
      "schema": "ApiKey",
      "description": "User generated API keys with security hashes and usage metrics"
    },
    "/{collection}/{documentId}": {
      "schema": "DynamicRecord",
      "description": "Flexible collections accommodating summaries, telemetry, and future application features"
    }
  }
}
